require "spec_helper"

describe PixijsStarter do
  it "has a version number" do
    expect(PixijsStarter::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
